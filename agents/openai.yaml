interface:
  display_name: "AssetShelf Render"
  short_description: "Render models and explicit rigid-part poses"
  default_prompt: "Use $assetshelf-render to inspect rigid FBX/GLB parts before any requested subobject rotation, select canonical paths without guessing, render only the explicit poses requested, and verify PNG or indexed contact-sheet output."
